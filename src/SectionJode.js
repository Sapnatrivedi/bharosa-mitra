import React, { useState } from 'react';
import BankDetails from './BankDetails';

const SectionJode = () => {
	return (
		<>
			<div className='float-end mt-3'>
				<button type='button' className='add-document'>
					Bank jode
				</button>
			</div>
		</>
	);
};

export default SectionJode;
